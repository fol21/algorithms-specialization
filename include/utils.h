#if !defined(UTILS_H)
#define UTILS_H

#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <algorithm>
#include <math.h>
#include <sys/time.h>
#include <time.h>

using namespace std;

vector<int> getInput(string path);

void print_vector(vector<int> vec);

#endif // UTILS_H
